apiVersion: v1
kind: Service
metadata:
  name: container-platform-api-service
  labels:
    app: container-platform-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: container-platform-api
